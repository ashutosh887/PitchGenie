const appConfig = {
  appName: "PitchGenie",
  appDescription: "AI that crafts the perfect pitch and follow-up!",
  redirectAfterSignIn: process.env.NEXT_PUBLIC_CLERK_AFTER_SIGN_IN_URL,
};

export default appConfig;
